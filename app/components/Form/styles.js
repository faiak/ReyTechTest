import { COLORS } from 'app/config/styles';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputWrap: { marginBottom: 16 },
  errorText: { color: COLORS.ERROR, paddingTop: 4 },
});

export default styles;
