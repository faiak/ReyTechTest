import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  flex: { flex: 1 },
  scroll: { flexGrow: 1, padding: 16 },
  scrollContent: { justifyContent: 'center', flex: 1 },
});

export default styles;
